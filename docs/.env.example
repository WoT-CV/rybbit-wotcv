# Public browser token for the standalone documentation globe.
# Optional: copy to docs/.env.local to override the built-in WoT-CV default.
# NEXT_PUBLIC_* values are embedded at build time. Use only a public (pk.) token
# and restrict its allowed URLs in the Mapbox account settings.
NEXT_PUBLIC_MAPBOX_TOKEN=pk.eyJ1Ijoid290LWN2IiwiYSI6ImNtcjZ3OGtyMDBsZWsyenM5aWliNmEyYW8ifQ.UlyVi6ufRJ4dmIfBzbpHHw
